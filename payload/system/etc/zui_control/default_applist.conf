# AppOpt default configuration for ZuiControl.
# Lines starting with # are examples and do not take effect.
# Package-wide presets only. Thread names and free-form CPU sets are rejected.
# Supported values: 0-7, 0-4, 5-7, 0-1
# Example only; use the ZuiControl UI to create a real rule:
# com.kurogame.mingchao=0-7
