# CPU Knob Ownership

Result: **NOT EXECUTED — blocked by Boot Hard Gate FAIL.**

The 20-transition balance/performance/fast/powersave time series and writer-trace A/B were not
started. `core_ctl`, input boost, and cpuset ownership remain SHARED/UNPROVEN; this run does not
change the static boundary that Uperf sched is disabled and asoulOpt is intended to remain the
per-task owner.
